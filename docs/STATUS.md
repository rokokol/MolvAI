# Состояние

Что работает прямо сейчас, а что ещё нет. Файл обновляется на каждом чекпоинте и намеренно перечисляет отрезанное: список того, чего нет, полезнее списка обещаний

## Как заполнять

Один раздел на чекпоинт, свежий сверху. В разделе четыре пункта: что заработало, что отрезано и почему, результат `just check` и результат `just falsify`. Формулировка «почти готово» запрещена — либо функция работает у пользователя, либо она в списке отрезанного

---

## 2026-09-05, 23:58 — MVP слит, идёт ночной блок

**Работает** (проверено пользователем на Hyprland вживую)

- Сквозная диктовка: бинд композитора (`F9` удержание, `Super+Alt+Z` переключатель) → `molva record start/stop` → демон → cpal → whisper.cpp → словарь → правила → вставка → JSONL-журнал. Текст появляется в активном окне
- Все шесть дорожек слиты в `main`: A захват и whisper, B GUI на Tauri 2, C CI/лицензии/доки/install.sh, D правила/словарь/стили/LLM/журнал/статистика/конфиг, E декодер файлов/модели/WER/bench, F демон/IPC/цепочка вставки/хоткеи. 610 тестов, `just check` зелёный
- CUDA-сборка через `nix develop .#cuda --command cargo build --release -p molva --features molva-core/cuda`: реплика 4 с распознаётся за 0,5–0,9 с вместе с загрузкой модели против 4 с на CPU; бинарь несёт rpath на драйвер и toolkit и работает вне оболочки
- Автоопределение языка (`stt.language = "auto"`) на GPU дешёвое; на CPU оно в 5 раз дороже фиксированного, поэтому по умолчанию `ru`
- GUI запускается (`cd crates/molva-gui && cargo tauri dev`), трей регистрируется в StatusNotifierWatcher (хост waybar)
- Лидерборд организаторов, прогон 07 в 18:31: первое место, 272 критерия из 296 автоматических

**Отрезано или не готово**

- Черновик во время записи, нарезка по паузам и дешёвое определение языка среди `allowed_languages` — дорожка I, в работе
- Звуковой сигнал, пауза перед вставкой, вырезание think-тегов из ответа модели, `history paste`, статичная иконка трея, приватность микрофона как явный признак — дорожка H, в работе
- Жёсткий линтер (pedantic, запрет `unwrap`/`panic` вне тестов, cargo-machete) и энтропия имён — дорожка G, в работе
- Тег `v0.1.0-alpha` не поставлен: ждёт слияния G/H/I
- LLM-клиент есть и протестирован против mock-сервера, но на живом Ollama не гонялся; `llm.enabled = false` по умолчанию
- CUDA в релизные артефакты не идёт: toolkit несвободный, только opt-in сборка руками

**Подводные камни, найденные за день** (каждый стоил от 20 минут до часа)

1. Под Nix ggml собирался без SIMD: `SOURCE_DATE_EPOCH` выключает `GGML_NATIVE`, реплика 4 с шла 84 с. Лечение: `GGML_NATIVE=ON` в devShell, переносимый x86-64-v3 (`GGML_AVX2/FMA/F16C`) в пакете и CI
2. `stt.language = "auto"` в whisper.cpp на CPU в 5 раз дороже фиксированного языка (23 с против 4 с)
3. `detect_language = true` в whisper.cpp значит «определи язык и выйди»: текста не будет вовсе
4. Энкодер whisper всегда считает окно 30 с: `audio_ctx` под длину реплики даёт 4× на CPU
5. Готовые биндинги whisper-rs-sys не совпадают по ABI с MSVC: на Windows биндинги генерируются libclang (`WHISPER_DONT_GENERATE_BINDINGS` только на Linux/macOS)
6. Раннер Windows выкачивал `markers/default.txt` с CRLF, пустая строка становилась маркером `\r` и гейт падал на здоровом прогоне: `.gitattributes` с `eol=lf`, плюс исправление в самом харнессе
7. macOS: bash 3.2 не знает `[[ -v ]]` и `declare -A`: харнесс исправлен upstream
8. Именованные каналы Windows принимают только `\\.\pipe\…`: путь сокета из tempdir отображается на имя канала
9. cargo-deny: `Unlicense` у whisper-rs, `CDLA-Permissive-2.0` у корневых сертификатов, path-зависимости workspace как wildcard (`publish = false`), gtk-rs 0.18 из Tauri объявлен unmaintained (`unmaintained = "workspace"`)
10. `rustsec/audit-check` падал при нуле уязвимостей: ему нужны `checks: write`
11. actionlint: `echo` с обратными слешами (SC2028) → `printf`
12. Windows отвечает на закрытый порт после повторов SYN около секунды: тест «недоступен» получал таймаут
13. Чекер жюри читает код агентом и ищет «признаки»: имена функций и настроек должны называть критерий явно (`single_instance`, `sound_cue`, `mic_release`); мутационный балл считает и тестовые фикстуры
14. Метрика энтропии имён (AC-10) роняется короткими именами (`id`, `err`, `cfg`), а не длинными: сокращать имена тестов вредно

**`just check`** — зелёный (610 тестов). **`just falsify`** — 52 дефекта на момент слияния D, все пойманы; после слияния E/F/GUI полный прогон не повторялся

**Дальше:** слить G/H/I → `just check` и `just falsify` → тег `v0.1.0-alpha` → живой Ollama и стиль `cleanup` → репетиция 12:30 → `v0.1.0` к 14:00 → заморозка 15:00

## 2026-09-05, 17:35 — каркас и CI

**Работает**

- Ядро `molva-core`: конфигурация в TOML со значениями по умолчанию, доменные типы аудио, текста, реплик, журнала и вставки, протокол IPC. 39 тестов
- Гейт `just check`: `fmt`, `clippy -D warnings`, тесты через `scripts/t.sh`, проверка SPDX-заголовков, проверка на `todo!`/`unimplemented!`
- CI: `ci.yml` — гейт на ubuntu-24.04, windows-latest и macos-latest плюс покрытие артефактом; `deny.yml` — лицензии и уязвимости как детектор по расписанию; `release.yml` — сборка по тегу `v*`
- Лицензии: `THIRD-PARTY.md` из cargo-about, `NOTICE`, SBOM в CycloneDX (`just sbom`), лицензии весов отдельным документом
- Фикстуры: две речевые записи автора под CC0, синтетические тишина и тон
- `nix run github:rokokol/MolvAI` собирает и запускает CLI без клонирования; `nix build .#molva` проверен
- `./install.sh` ставит в `~/.local` без прав администратора, `--uninstall` снимает по манифесту и печатает отчёт

**Отрезано на этом чекпоинте**

- GUI на Tauri 2 ещё не в репозитории: шаги CI и релиза для него написаны и включаются по появлению `crates/molva-gui/tauri.conf.json` и `crates/molva-gui/ui/package.json`
- `install.sh` ставит собранный бинарник, но не умеет скачивать релизный архив: релизов ещё нет
- Своего `install.ps1` нет; на Windows пока сборка `cargo build --release` руками или готовый архив из релиза
- Проверка гейта на Windows и macOS не наблюдалась вживую — до первого прогона в Actions это утверждение, а не факт

**`just check`** — зелёный локально в `nix develop`

**`just falsify`** — 7 записей в `tests/defects.sh`, сьют поймал все 7

**Проверки, увиденные красными** — `just spdx-check` краснеет на файле без заголовка, `just no-stubs` — на добавленном `todo!()`, шаг «инструменты из lock-файлов» — на подложенном незакреплённом `cargo install`. Каждая проверка после этого возвращалась в зелёное на восстановленном дереве

---

## Шаблон следующей записи

```markdown
## ДД.ММ.ГГГГ, ЧЧ:ММ — заголовок чекпоинта

**Работает**
- …

**Отрезано на этом чекпоинте**
- …

**`just check`** — зелёный / красный, чем именно

**`just falsify`** — N записей, все пойманы / пережиты такие-то
```
